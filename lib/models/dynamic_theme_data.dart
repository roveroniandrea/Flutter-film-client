import 'dart:ui';

/// Proprietà per il tema material design
class DynamicThemeData {
  /// Colore primario dell'app
  final Color primaryColor;

  /// Colore di contrasto
  final Color accentColor;

  DynamicThemeData(this.primaryColor, this.accentColor);
}
